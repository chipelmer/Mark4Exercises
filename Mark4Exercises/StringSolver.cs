using System;

namespace Mark4Exercises
{
    public class StringSolver
    {
        public string FullName(string firstName, string lastName)
        {
            // return a full name from a first and last name
            // if only one name is given, return that name
            // return an empty string for edge cases
            return "";
        }

        public string MakeUppercase(string value)
        {
            // return a string converted to uppercase
            // return an empty string for edge cases
            return "";
        }

        public bool AreEqual(string value1, string value2)
        {
            // return true if two strings are the same, including casing
            // return false for edge cases (including 2 null values)
            return false;
        }

        public bool AreEqualIgnoreCase(string value1, string value2)
        {
            // return true if two strings are the same, regardless of casing
            // return false for edge cases (including 2 null values)
            return false;
        }
    }
}
