using System;

namespace Mark4Exercises
{
    public class ArraySolver
    {
        public int Sum(int[] numbers)
        {
            // return the total of all elements in the array
            // return 0 for edge cases
            return 0;
        }

        public int SumNegativeNumbers(int[] numbers)
        {
            // return the sum of only the negative elements in the array
            // return 0 for edge cases
            return 0;
        }

        public double Average(int[] numbers)
        {
            // return the average of all elements in the array
            // return 0 for edge cases
            return 0;
        }

        public bool ContainsTrue(bool[] values)
        {
            // return true if the array contains at least one true value
            // return false for edge cases
            return false;
        }

        public bool ContainsEven(int[] numbers)
        {
            // return true if the array contains at least one even number
            // return false for edge cases
            return false;
        }

        public int[] GetPositiveEvenNumbers(int maxNumber)
        {
            // return all positive even numbers that are less than or equal to maxNumber
            // numbers should be in ascending order
            // return an empty array for edge cases
            return new int[0];
        }

        public string[] MakeElementsUppercase(string[] values)
        {
            // convert each element in the array to be uppercase
            // null elements should remain null
            // return the original array for edge cases
            return values;
        }
    }
}
